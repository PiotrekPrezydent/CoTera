<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CoTera.MainPage">

    <Grid RowDefinitions="50,.80*,50"
          ColumnDefinitions=".20*,.60*,.20*"
          Padding="10"
          RowSpacing="10"
          ColumnSpacing="10">

        <Button
            Grid.Row="0"
            Grid.Column="0"
            Text="////"
            Clicked="OnPreviousClicked"/>

        <Label
            x:Name="Lab_DOTW"
            Grid.Row="0"
            Grid.Column="1"
            Text="MONDAY"/>

        <Button 
            Grid.Row="0"
            Grid.Column="2"
            Text="\\\\"
            Clicked="OnNextClicked"/>

        <CollectionView Grid.Row="1" Grid.ColumnSpan="3" x:Name="ClassesList">
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Test</x:String>
                    <x:String>Test</x:String>
                    <x:String>Test</x:String>
                    <x:String>Test</x:String>
                    <x:String>Test</x:String>
                </x:Array>
            </CollectionView.ItemsSource>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,5">
                        <Frame>
                            <Label Text="{Binding .}"
                                   FontSize="24" />
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        <Button 
            Grid.Row="2"
            Grid.Column="0"
            Text="Refresh"/>
        <Button 
            Grid.Row="2"
            Grid.Column="2"
            Text="Options"/>

    </Grid>

</ContentPage>
